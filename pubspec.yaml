name: outfit
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: '>=2.18.2 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_fonts: ^4.0.3
  share_plus: ^6.3.1
  flutter_localizations:
    sdk: flutter
  reusables:
    git: https://github.com/SparkoSol/reusables.git
  flutter_svg: ^1.1.6
  provider: ^6.0.5
  hive: ^2.2.3
  hive_flutter: ^1.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/languages/